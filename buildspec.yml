version: 0.2

env:
  shell: bash

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - python -m pip install --upgrade pip
      - python -m pip install -r requirements.txt

  pre_build:
    commands:
      # A non-zero pytest exit stops CodeBuild, so no image is built or pushed.
      - python -m pytest -v
      - TESTS_PASSED=true
      - test -n "$AWS_ACCOUNT_ID"
      - test -n "$AWS_DEFAULT_REGION"
      - test -n "$IMAGE_REPO_NAME"
      - REPOSITORY_URI="$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME"
      - COMMIT_TAG="$(printf '%s' "$CODEBUILD_RESOLVED_SOURCE_VERSION" | cut -c 1-12)"
      - test -n "$COMMIT_TAG"
      - aws ecr get-login-password --region "$AWS_DEFAULT_REGION" | docker login --username AWS --password-stdin "$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com"

  build:
    commands:
      - test "$TESTS_PASSED" = "true"
      - docker build --file Dockerfile.backend --tag "$REPOSITORY_URI:$COMMIT_TAG" --tag "$REPOSITORY_URI:latest" .

  post_build:
    commands:
      - test "$TESTS_PASSED" = "true"
      - docker push "$REPOSITORY_URI:$COMMIT_TAG"
      - docker push "$REPOSITORY_URI:latest"
      - printf '{"repositoryUri":"%s","imageTag":"%s","imageUri":"%s"}\n' "$REPOSITORY_URI" "$COMMIT_TAG" "$REPOSITORY_URI:$COMMIT_TAG" > image-detail.json

artifacts:
  files:
    - image-detail.json
